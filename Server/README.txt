This folder contains the Web-App-Server.

To run the server:
(cmd):
	npm start

Dependencies (npm):
	npm install --save -g 
	npm install --save -g 
	npm install --save -g 
	npm install --save -g 
	npm install --save -g 
	npm install --save -g 